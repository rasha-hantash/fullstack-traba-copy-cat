"use client";
function classNames(...classes: string[]) {
    return classes.filter(Boolean).join(" ");
}

export default function MainContainer() {
    return (
        <div className="flex-auto border rounded-md m-2 ">
            {/* todo: create a search bar component */}
            
        </div>
    );
}